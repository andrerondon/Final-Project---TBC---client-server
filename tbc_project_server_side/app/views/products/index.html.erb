<h1>All Products</h1>
<% @products.each do |product| %>
    <ul class="list-group">
        <li class="list-group-item"><%= link_to product.name, product_path(product) %></li>
        <li class="list-group-item"><%= link_to product.img_url, product_path(product) %></li>
    </ul>
<% end %>